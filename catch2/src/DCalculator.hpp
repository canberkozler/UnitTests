#pragma once

class DCalculator {
public:
    double add(double a, double b) const {
        return a + b;
    }
    double multiply(double a, double b) const {
        return a * b;
    }
};